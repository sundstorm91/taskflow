meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  artifacts:
    - path: .next
    - path: prisma/schema.prisma
  commands:
    - npm install
    - npx prisma generate
    - npm run build
run:
  command: npm start
  port: 3000
